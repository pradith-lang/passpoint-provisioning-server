# Certificates

Place CA certificate files here if your Passpoint profile requires certificate-based validation.

For EAP-SIM / EAP-AKA lab profiles, this folder may remain empty.
If you add a CA certificate, update `ca_file` in `main.py` for the relevant profile.
